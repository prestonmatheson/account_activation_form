require 'httparty'
class AccountActivator
  def intialize
    # @headers => some content
  end

  def activate_account(user_id)
    # accept a single user_id and pass it into the url for account activation to the taxbot.
  end
end